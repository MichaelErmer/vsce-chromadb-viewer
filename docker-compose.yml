version: "3.8"

services:
  chroma:
    image: chromadb/chroma:latest
    container_name: chroma-test
    ports:
      - "8000:8000"
    volumes:
      - ./docker/chroma/data:/data
    environment:
      - CHROMA_DB_IMPL=duckdb+parquet
    restart: unless-stopped
